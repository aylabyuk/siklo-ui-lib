import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="Design Tokens" />

# Design Tokens

Tokens are the atomic values that drive every component's appearance. They flow through a pipeline:

**Figma (Tokens Studio) → JSON Export → Style Dictionary → CSS Variables + TS Constants**

---

## Colors

### Primitives

<div
  style={{
    display: 'grid',
    gridTemplateColumns: 'repeat(5, 1fr)',
    gap: '0.5rem',
    marginBottom: '2rem',
  }}
>
  {['50', '100', '200', '300', '400', '500', '600', '700', '800', '900'].map(
    (shade) => (
      <div
        key={`blue-${shade}`}
        style={{
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          gap: '0.25rem',
        }}
      >
        <div
          style={{
            width: '3rem',
            height: '3rem',
            borderRadius: '0.375rem',
            backgroundColor: `var(--colors-blue-${shade})`,
            border: '1px solid var(--semantic-border)',
          }}
        />
        <span
          style={{
            fontSize: '0.7rem',
            color: 'var(--semantic-muted-foreground)',
          }}
        >
          blue-{shade}
        </span>
      </div>
    ),
  )}
</div>

<div
  style={{
    display: 'grid',
    gridTemplateColumns: 'repeat(5, 1fr)',
    gap: '0.5rem',
    marginBottom: '2rem',
  }}
>
  {[
    '50',
    '100',
    '200',
    '300',
    '400',
    '500',
    '600',
    '700',
    '800',
    '900',
    '950',
  ].map((shade) => (
    <div
      key={`slate-${shade}`}
      style={{
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        gap: '0.25rem',
      }}
    >
      <div
        style={{
          width: '3rem',
          height: '3rem',
          borderRadius: '0.375rem',
          backgroundColor: `var(--colors-slate-${shade})`,
          border: '1px solid var(--semantic-border)',
        }}
      />
      <span
        style={{
          fontSize: '0.7rem',
          color: 'var(--semantic-muted-foreground)',
        }}
      >
        slate-{shade}
      </span>
    </div>
  ))}
</div>

### Semantic Colors

<div
  style={{
    display: 'grid',
    gridTemplateColumns: 'repeat(4, 1fr)',
    gap: '1rem',
    marginBottom: '2rem',
  }}
>
  {[
    { name: 'primary', var: '--semantic-primary' },
    { name: 'secondary', var: '--semantic-secondary' },
    { name: 'destructive', var: '--semantic-destructive' },
    { name: 'success', var: '--semantic-success' },
    { name: 'warning', var: '--semantic-warning' },
    { name: 'muted', var: '--semantic-muted' },
    { name: 'accent', var: '--semantic-accent' },
    { name: 'border', var: '--semantic-border' },
  ].map((color) => (
    <div
      key={color.name}
      style={{ display: 'flex', alignItems: 'center', gap: '0.75rem' }}
    >
      <div
        style={{
          width: '2.5rem',
          height: '2.5rem',
          borderRadius: '0.375rem',
          backgroundColor: `var(${color.var})`,
          border: '1px solid var(--semantic-border)',
          flexShrink: 0,
        }}
      />
      <div>
        <div style={{ fontSize: '0.875rem', fontWeight: 500 }}>
          {color.name}
        </div>
        <div
          style={{
            fontSize: '0.7rem',
            color: 'var(--semantic-muted-foreground)',
          }}
        >
          {color.var}
        </div>
      </div>
    </div>
  ))}
</div>

---

## Typography

<div
  style={{
    display: 'flex',
    flexDirection: 'column',
    gap: '1rem',
    marginBottom: '2rem',
  }}
>
  {[
    {
      label: 'Display 2XL',
      size: '1.5rem',
      weight: 700,
      sample: 'The quick brown fox',
    },
    {
      label: 'XL Semibold',
      size: '1.25rem',
      weight: 600,
      sample: 'The quick brown fox',
    },
    {
      label: 'LG Medium',
      size: '1.125rem',
      weight: 500,
      sample: 'The quick brown fox',
    },
    {
      label: 'Base Regular',
      size: '1rem',
      weight: 400,
      sample: 'The quick brown fox jumps over the lazy dog',
    },
    {
      label: 'SM',
      size: '0.875rem',
      weight: 400,
      sample: 'The quick brown fox jumps over the lazy dog',
      muted: true,
    },
    {
      label: 'XS',
      size: '0.75rem',
      weight: 400,
      sample: 'The quick brown fox jumps over the lazy dog',
      muted: true,
    },
  ].map((item) => (
    <div
      key={item.label}
      style={{
        display: 'flex',
        alignItems: 'baseline',
        gap: '1rem',
      }}
    >
      <span
        style={{
          fontSize: item.size,
          fontWeight: item.weight,
          fontFamily: 'Inter, system-ui, sans-serif',
          color: item.muted ? 'var(--semantic-muted-foreground)' : 'inherit',
          whiteSpace: 'nowrap',
        }}
      >
        {item.label} — {item.sample}
      </span>
      <span
        style={{
          fontSize: '0.7rem',
          color: 'var(--semantic-muted-foreground)',
          whiteSpace: 'nowrap',
        }}
      >
        {item.size}
      </span>
    </div>
  ))}
</div>

<table
  style={{ width: '100%', borderCollapse: 'collapse', fontSize: '0.875rem' }}
>
  <thead>
    <tr style={{ borderBottom: '2px solid var(--semantic-border)' }}>
      <th
        style={{ textAlign: 'left', padding: '0.75rem 1rem', fontWeight: 600 }}
      >
        Token
      </th>
      <th
        style={{ textAlign: 'left', padding: '0.75rem 1rem', fontWeight: 600 }}
      >
        Value
      </th>
      <th
        style={{ textAlign: 'left', padding: '0.75rem 1rem', fontWeight: 600 }}
      >
        CSS Variable
      </th>
    </tr>
  </thead>
  <tbody>
    {[
      ['font-size-xs', '0.75rem', '--font-size-xs'],
      ['font-size-sm', '0.875rem', '--font-size-sm'],
      ['font-size-base', '1rem', '--font-size-base'],
      ['font-size-lg', '1.125rem', '--font-size-lg'],
      ['font-size-xl', '1.25rem', '--font-size-xl'],
      ['font-size-2xl', '1.5rem', '--font-size-2xl'],
    ].map(([token, value, cssVar]) => (
      <tr
        key={token}
        style={{ borderBottom: '1px solid var(--semantic-border)' }}
      >
        <td style={{ padding: '0.5rem 1rem' }}>
          <code>{token}</code>
        </td>
        <td style={{ padding: '0.5rem 1rem' }}>{value}</td>
        <td style={{ padding: '0.5rem 1rem' }}>
          <code>{cssVar}</code>
        </td>
      </tr>
    ))}
  </tbody>
</table>

---

## Spacing

<div
  style={{
    display: 'flex',
    alignItems: 'flex-end',
    gap: '0.75rem',
    marginBottom: '2rem',
  }}
>
  {[
    { name: '1', size: '0.25rem' },
    { name: '2', size: '0.5rem' },
    { name: '3', size: '0.75rem' },
    { name: '4', size: '1rem' },
    { name: '5', size: '1.25rem' },
    { name: '6', size: '1.5rem' },
    { name: '8', size: '2rem' },
    { name: '10', size: '2.5rem' },
    { name: '12', size: '3rem' },
    { name: '16', size: '4rem' },
  ].map((space) => (
    <div
      key={space.name}
      style={{
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        gap: '0.25rem',
      }}
    >
      <div
        style={{
          width: space.size,
          height: space.size,
          backgroundColor: 'var(--semantic-primary)',
          borderRadius: '2px',
        }}
      />
      <span
        style={{
          fontSize: '0.65rem',
          color: 'var(--semantic-muted-foreground)',
        }}
      >
        {space.name}
      </span>
    </div>
  ))}
</div>

---

## Border Radius

<div style={{ display: 'flex', gap: '1.5rem', marginBottom: '2rem' }}>
  {[
    { name: 'sm', value: '0.25rem' },
    { name: 'md', value: '0.375rem' },
    { name: 'lg', value: '0.5rem' },
    { name: 'xl', value: '0.75rem' },
    { name: 'full', value: '9999px' },
  ].map((radius) => (
    <div
      key={radius.name}
      style={{
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        gap: '0.5rem',
      }}
    >
      <div
        style={{
          width: '3rem',
          height: '3rem',
          backgroundColor: 'var(--semantic-primary)',
          borderRadius: `var(--radii-${radius.name})`,
        }}
      />
      <span
        style={{
          fontSize: '0.7rem',
          color: 'var(--semantic-muted-foreground)',
        }}
      >
        {radius.name}
      </span>
    </div>
  ))}
</div>

---

## Shadows

<div style={{ display: 'flex', gap: '2rem', marginBottom: '2rem' }}>
  {['sm', 'md', 'lg', 'xl'].map((shadow) => (
    <div
      key={shadow}
      style={{
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        gap: '0.5rem',
      }}
    >
      <div
        style={{
          width: '5rem',
          height: '5rem',
          backgroundColor: 'var(--semantic-background)',
          borderRadius: 'var(--radii-lg)',
          boxShadow: `var(--shadow-${shadow})`,
        }}
      />
      <span
        style={{
          fontSize: '0.7rem',
          color: 'var(--semantic-muted-foreground)',
        }}
      >
        {shadow}
      </span>
    </div>
  ))}
</div>

---

## Motion

<table
  style={{ width: '100%', borderCollapse: 'collapse', fontSize: '0.875rem' }}
>
  <thead>
    <tr style={{ borderBottom: '2px solid var(--semantic-border)' }}>
      <th
        style={{ textAlign: 'left', padding: '0.75rem 1rem', fontWeight: 600 }}
      >
        Token
      </th>
      <th
        style={{ textAlign: 'left', padding: '0.75rem 1rem', fontWeight: 600 }}
      >
        Value
      </th>
      <th
        style={{ textAlign: 'left', padding: '0.75rem 1rem', fontWeight: 600 }}
      >
        CSS Variable
      </th>
    </tr>
  </thead>
  <tbody>
    {[
      ['duration-fast', '100ms', '--duration-fast'],
      ['duration-normal', '200ms', '--duration-normal'],
      ['duration-slow', '300ms', '--duration-slow'],
      ['easing-ease-in', 'cubic-bezier(0.4, 0, 1, 1)', '--easing-ease-in'],
      ['easing-ease-out', 'cubic-bezier(0, 0, 0.2, 1)', '--easing-ease-out'],
      [
        'easing-ease-in-out',
        'cubic-bezier(0.4, 0, 0.2, 1)',
        '--easing-ease-in-out',
      ],
    ].map(([token, value, cssVar]) => (
      <tr
        key={token}
        style={{ borderBottom: '1px solid var(--semantic-border)' }}
      >
        <td style={{ padding: '0.5rem 1rem' }}>
          <code>{token}</code>
        </td>
        <td style={{ padding: '0.5rem 1rem' }}>{value}</td>
        <td style={{ padding: '0.5rem 1rem' }}>
          <code>{cssVar}</code>
        </td>
      </tr>
    ))}
  </tbody>
</table>
